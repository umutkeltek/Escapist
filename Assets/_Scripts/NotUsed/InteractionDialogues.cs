using System.Collections;
using System.Collections.Generic;
using TMPro;
using UnityEngine;
using UnityEngine.UI;
using TMPro;
public class InteractionDialogues : MonoBehaviour
{
    [SerializeField] TMP_Text[] rightclick = new TMP_Text[5];
    [SerializeField] TMP_Text[] leftclick = new TMP_Text[5];
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
